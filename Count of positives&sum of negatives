const countPositivesSumNegatives = (input) =>{
  if(input == null  || input.length == 0) return [];
  else{
      let positiveCounter =0 , negativeCounter=0;
      input.map( (m) => m>0 ? positiveCounter++ : negativeCounter+=m);  
      return [positiveCounter , negativeCounter];
   }
}
