function fakeBin(x){
  
  let arr =x.split("")
  for(let i=0;i<x.length;i++)
    {
      if(arr[i] <5)
      arr.splice(i,1,'0');
      else
      arr.splice(i,1,'1');
      

    }
        return arr.join("");

}
