function solution(number){
  if(number <=0)
    return 0;
  else
    {
      var sum=0;
      
        for(var i =1;i<=number-1;i++)
        {
            if(i % 3===0 || i%5===0)
              sum+=i;
        }
    }
  return sum 
}
