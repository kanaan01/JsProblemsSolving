function sum (numbers)
{ 
  let sum =0;
   if(numbers ==[]) return 0
  else
    {
    for(let i=0;i<numbers.length;i++)
      {
        sum+=numbers[i]
      }
    }
  return sum;
};

// short hand silution using reduce
function sum(numbers) {
  return numbers.reduce((a, b)=> a+b , 0)
}
