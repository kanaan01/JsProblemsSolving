function SeriesSum(n)
{
  let sum=1 , c=4;
   if(n === 0)
     return "0.00";
 else{
  for(let i =1;i<n;i++)
    {
      sum = sum  + (1/c)
      c=c+3;
    }
   let result = sum.toPrecision(3);
   return result+"";
   }
}
