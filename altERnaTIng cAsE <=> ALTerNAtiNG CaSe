String.prototype.toAlternatingCase = function () {
  
  return [...this].map(
    
    (key) => ((key === key.toUpperCase())? key.toLowerCase() : key.toUpperCase()),
   
                       
  ).join("");

}
